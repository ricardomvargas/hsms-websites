# Copy this file to .env and adjust as needed.
# Every variable is optional — the defaults in app/config.py are used when unset.

DB_PATH=hsm.db
DNS_LIFETIME=5
HTTP_TIMEOUT=8
WIKI_TIMEOUT=10
CORS_ORIGINS=["http://localhost:5173"]
IMPORT_RATE_LIMIT=3/hour
LOG_LEVEL=INFO
